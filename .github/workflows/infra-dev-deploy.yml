name: infra-dev-deploy

on:
  pull_request:
    types: [synchronize, reopened, opened, ready_for_review]

jobs:
  infra-dev-deploy:
    uses: shortcake-dev/shortcake-terraform/.github/workflows/tf-deploy.yml@003758781fee8ee5be688f64667a60ea498470db
    with:
      release_type: "dev"
      release_name: "infra-pr${{ github.event.number }}"
