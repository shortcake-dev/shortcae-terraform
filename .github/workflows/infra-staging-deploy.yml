name: infra-staging-deploy

on:
  push:
    branches:
      - main

jobs:
  infra-staging-deploy:
    uses: shortcake-dev/shortcake-terraform/.github/workflows/tf-deploy.yml@main
    with:
      release_type: "staging"
      release_name: "infra"
